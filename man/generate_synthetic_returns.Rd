% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulations_models.R
\name{generate_synthetic_returns}
\alias{generate_synthetic_returns}
\title{Generate a single synthetic stock return path using a stochastic volatility model with jumps and regime switching.}
\usage{
generate_synthetic_returns(
  n_days = 252 * 10,
  mu = 2e-04,
  kappa = 0.05,
  theta = 1e-04,
  sigma_v = 0.01,
  rho = -0.7,
  lambda_jump = 0.05,
  jump_size_dist = "normal",
  sigma_jump = 0.02,
  noise_dist = "normal",
  noise_scale = 5e-04,
  noise_df = 5,
  regime_params = NULL,
  random_seed = NULL
)
}
\arguments{
\item{n_days}{Number of trading days to simulate (default: 252 * 10).}

\item{mu}{Drift (mean return) per time step.}

\item{kappa}{Mean-reversion speed of the variance process.}

\item{theta}{Long-run average variance.}

\item{sigma_v}{Volatility of volatility (vol of variance process).}

\item{rho}{Correlation between volatility and returns (leverage effect).}

\item{lambda_jump}{Jump intensity (expected jumps per day).}

\item{jump_size_dist}{Distribution of jump sizes ("normal", "log_normal", or "exponential").}

\item{sigma_jump}{Scale parameter for jump size distribution.}

\item{noise_dist}{Type of microstructure noise ("normal" or "student_t").}

\item{noise_scale}{Standard deviation (or scale) of added microstructure noise.}

\item{noise_df}{Degrees of freedom for Student-t noise (if used).}

\item{regime_params}{List specifying regime switching behavior. Includes a transition matrix, and multipliers for `theta` and `kappa` during high-volatility regimes.}

\item{random_seed}{Optional random seed for reproducibility.}
}
\value{
A `data.table` with columns: `date`, `returns`, `variance`, `volatility`, `regime`.
}
\description{
This function simulates returns based on a Heston-style stochastic volatility process,
optionally incorporating jumps (with configurable distributions), microstructure noise,
and regime switching via a Markov chain.
}
